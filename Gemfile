source 'https://rubygems.org'

gem 'rake', '0.8.2'

gem 'rails', '2.1.2'

# gem 'rails', '~> 2.3.17', :require => nil

gem 'sqlite3-ruby', :require => 'sqlite3'

group :development do
  gem 'rails-footnotes'
end

group :test do
  gem 'rspec'
end

######################################################################
# STOPHERE
######################################################################
# Devise 1.0.2 is not a valid gem plugin for Rails, so use git
#    until 1.0.3.
# gem 'devise', :git => 'git://github.com/plataformatec/devise.git',
#    :ref => 'v1.0'
